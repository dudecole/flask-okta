# flask-okta

## Vagrant Pre-Reqs
1. Install the vagrant docker-compose plugins: 
     vagrant plugin install vagrant-docker-compose
2. Run: 
     vagrant up <optional>  (optional vm name or all as default.  1. web - is just the flask app
     								  2. docker - is the flask app containerized

